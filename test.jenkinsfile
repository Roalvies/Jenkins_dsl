pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh 'apt install wget'
        sh 'apt install curl'
      }
    }

    stage('Test') {
      steps {
        sh 'apt install vim'
      }
    }
  }
}
